# Runtime dependencies for the inference API and dashboard.
# Kept deliberately narrow so the serving container stays small: training-only
# tooling (ZenML, DVC, Evidently, pandera) lives in requirements-dev.txt.
numpy>=1.26,<2.0
pandas>=2.2,<3.0
scikit-learn==1.6.1  # must match the version the pipeline was pickled with
xgboost>=2.0,<3.0
shap>=0.46,<1.0
joblib>=1.3,<2.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
streamlit>=1.38,<2.0
requests>=2.32,<3.0
openpyxl>=3.1,<4.0
