# Training, validation, monitoring, and quality tooling.
# Installing this file also installs everything in requirements.txt.
-r requirements.txt

zenml>=0.68,<1.0
# ZenML's own metadata store dependencies, which its wheel under-declares.
# Without these, `zenml init` and the pipeline run both fail on import.
pymysql>=1.1,<2.0
sqlmodel>=0.0.22
passlib[bcrypt]>=1.7
sqlalchemy-utils>=0.41

mlflow>=2.16,<3.0
dvc>=3.55,<4.0
pandera>=0.20,<1.0
evidently>=0.4.40,<1.0
matplotlib>=3.9,<4.0

pytest>=8.3,<9.0
httpx>=0.27,<1.0
ruff>=0.6,<1.0
pre-commit>=3.8,<5.0
